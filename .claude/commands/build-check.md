빌드 전 전체 검사를 실행합니다. (타입체크 → 린트 → 빌드)

아래 명령어를 **순서대로** 실행하고, 각 단계의 결과를 요약해서 보여주세요:

1. `pnpm typecheck`
2. `pnpm lint`
3. `pnpm build`

각 단계에서 에러가 발생하면:
- 에러 내용을 파일명:줄번호 형태로 정리
- 다음 단계는 실행하지 않고 에러 수정 방법을 제안

모든 단계가 성공하면 "빌드 검사 통과" 메시지를 출력하세요.
